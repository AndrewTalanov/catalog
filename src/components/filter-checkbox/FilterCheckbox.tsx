import styles from "./FilterCheckbox.module.scss";
import { FilterCheckboxType } from "./FilterCheckboxType";

const FilterCheckbox: React.FC<FilterCheckboxType> = ({}) => {
    return (
        <div></div>
    );
}

export default FilterCheckbox;